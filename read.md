Новый проект на Go. 

10.12.2025
Первый этап реализация чистого веб сервиса. На вход JSON на выход JSON

Добавлен докерфайл
    docker run -p (Порт хоста)8082:8081(порт контейнера указанный в EXPOSE) goprj001
Добавлен фронтенд из дипсика
Донастрона сборка билда на базе образа scratch (ключевой момент для использования scratch это билд бинарника вместе с нужными либами)

12.12.2025
Потратил целый день на то чтобы завернуть настройку веб сервера в кучу абстракций. хэндлеры обернуть интерфейсом для гибкой настройки.
В какой-то момент понял что хочется делать всё и сразу неимоверно усложнив. Подход оказался так себе.
Решено структурировать подход и начать с требований, реализовать на базовом функционале, а затем модифицировать.

Поехали agile:
Сервис сбора показаний счетчиков: электричество, газ.
Поля для газа: дата, числовое значение
Поля для электричества: дата, числовые значения день, ночь, сумма
Ввод значений свободный, просмотр истории только для авторизованного пользователя


Шото ору, устанавливая корсы узнал о существовании Middleware
добавил пару методов для принятия json с показаниями
вынес фронт в отдельный докер.

План добавить логирование и compose
Ну и запись показаний в рантайм

+Compose

Добавил структуру бд, поменял UI. Теперь данные хранятся в рантайме. Думаю писать их кудато или сначала за историю взяться.

14.12.2025
Добави простой вывод истории. теперь надо браться за валидацию. 


случилась внезапная пеерестройка структуры, а то бардак.
План такой:
БД - Интерфейс - Логика - Хэндлер - Сервер

Валидацию и логирование будем наматывать уже на эту структуру

15.12
начал добавлять ошибки в возвращаемых значениях.
Влепил дженерик для парсинга json
 Возвращаемся из бранча по рефакторингу структуры. 
 Было иинтересно. за одно типизировали ошибки